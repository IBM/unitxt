import os
CATALOG_DIR = os.path.join(os.path.dirname(os.path.dirname(os.path.abspath(__file__))),'src',
                               'unitxt','catalog')

AUGMENTABLE = 'augmentable_inputs'
LOADER_LIMIT = 'loader_limit'
{
    "type": "sequential_operator",
    "steps": [
        {
            "type": "extract_with_regex",
            "regex": "\\[\\[(.*?)\\]\\]",
            "field": "prediction",
            "process_every_value": false
        }
    ]
}
